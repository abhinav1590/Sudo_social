<h1>Posts</h1>
{{#each @model as |post|}}
  <div class="post">
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>Author: {{post.author.name}}</p>
    <p>Likes: {{post.likes.length}}</p>
    <p>Comments: {{post.comments.length}}</p>
  </div>
{{/each}}